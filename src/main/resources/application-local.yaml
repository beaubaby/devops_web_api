server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loan_eligibility
    username: postgres
    password: 12345
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        generate_statistics: true
        ddl-auto: validate
        jdbc:
          time_zone: UTC
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
